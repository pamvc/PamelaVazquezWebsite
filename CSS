
/*MY CODE*/

/*problems I have run into: 
The pink background container with the class name "name" changes size based on the window size. Which is good but I also need the background to change size based on the text that is on top of it. If you make the window smaller you will see that there comes a point where the pink background is smaller than the text
PROBLEM IS FIXED! */


.DRAWING{
    background-image: url(Drawings_copy%20copy.JPG);
    background-color: plum;
    padding: 200px; 
    background-size: 50vw;
    position: relative;
    clip-path: polygon(0 62%, 0% 0%, 100% 0%, 100% 62%, 51% 91%);
    
}

.name {
    position: sticky;
    align-content: stretch;
    background-color: transparent;
    padding: 25px;
}
.name p{
    
    font-family:'Fascinate', cursive;
    font-size: 5vw;
    text-align: center;
    color: black;
   
}

#under-name{
    font-size: 2vw;
}


        .content{
        background-color: white;
        }

.paragraph{
    padding-bottom: 25px;
    text-align: center;
    font-size: 20px;
    font-family: 'Fascinate', cursive;
    color: black;
}

.favorites{
    font-size: 20px;
    font-family: 'Fascinate', cursive;
    background-color: plum;
    padding: 150px;
    margin-top: 35px;
    text-align: center;
    align-items: center;
    clip-path: polygon(50% 0%, 100% 38%, 100% 100%, 0 100%, 0% 38%);
    min-width: 8vw;
}

.favorites li{
    padding-bottom: 15px;
    padding-left: 1em;
}

li span{
    padding-left: -0.5em;
}

        
 
